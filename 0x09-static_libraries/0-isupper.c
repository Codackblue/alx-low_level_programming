#include "main.h"

/**
 * _isupper - checks for uppercase chars
 * @c: the character to be checked in
 * Return: 1 if c is uppercase, 0 otherwise done
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
